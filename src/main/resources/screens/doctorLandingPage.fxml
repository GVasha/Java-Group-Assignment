<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<?import javafx.scene.control.DatePicker?>
<?import javafx.scene.control.TextField?>

<BorderPane prefHeight="600.0" prefWidth="900.0" style="-fx-background-color: #f0f2f5;" xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1" fx:controller="controllers.DoctorPageController">

    <!-- Top navbar -->
    <top>
        <HBox alignment="CENTER_LEFT" spacing="10.0" style="-fx-background-color: #ffffff;" BorderPane.alignment="CENTER">
            <padding>
                <Insets bottom="10" left="20" right="20" top="10" />
            </padding>
            <children>

                <!-- Left: portal title / greeting -->
                <Label fx:id="doctorGreetingLabel"
                       style="-fx-font-size: 22px; -fx-font-weight: bold; -fx-text-fill: #222222;"
                       text="Hello, Doctor" />

                <!-- Spacer to push buttons to the right -->
                <Region HBox.hgrow="ALWAYS" />

                <!-- Right: nav buttons (Book, My appointments, Profile) -->

                <MenuButton fx:id="profileMenuButton" text="Profile">
                    <items>
                        <MenuItem text="Modify Account Settings" onAction="#handleModifyAccount" />
                        <MenuItem text="Log out" onAction="#handleLogout" />
                        <MenuItem text="Delete account" onAction="#handleDeleteAccount" />
                    </items>
                </MenuButton>

            </children>
        </HBox>
    </top>

    <!-- Center content: title + appointments table in a card -->
    <center>
        <VBox alignment="TOP_CENTER" spacing="20.0">
            <padding>
                <Insets bottom="40" left="40" right="40" top="40" />
            </padding>
            <children>

                <!-- Main heading similar to "Hello, Patient" -->
                <Label style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #333333;" text="Your appointments" />

                <!-- Optional subtitle -->
                <Label style="-fx-font-size: 14px; -fx-text-fill: #666666;" text="Review and manage your upcoming and past visits." />

                <!-- Card containing the table -->
                <VBox spacing="10.0" style="-fx-background-color: #ffffff;                              -fx-background-radius: 12;                              -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.12), 12, 0, 0, 4);">
                    <padding>
                        <Insets bottom="16" left="16" right="16" top="16" />
                    </padding>
                    <children>

                        <HBox spacing="10">
                            <Label text="From:" />
                            <DatePicker fx:id="startDatePicker"/>

                            <Label text="To:" />
                            <DatePicker fx:id="endDatePicker"/>

                            <Label text="Patient Name:" />
                            <TextField fx:id="patientNameField" prefWidth="70"/>

                            <Button text="Filter" onAction="#handleFilter"/>
                            <Button text="Reset" onAction="#handleReset"/>
                        </HBox>

                        <TableView fx:id="appointmentsTable" prefHeight="350.0" prefWidth="800.0">
                            <columns>
                                <TableColumn fx:id="dateColumn" prefWidth="140.0" text="Date" />
                                <TableColumn fx:id="timeColumn" prefWidth="100.0" text="Time" />
                                <TableColumn fx:id="patientIdColumn" prefWidth="40.0" text="ID" />
                                <TableColumn fx:id="patientColumn" prefWidth="220.0" text="Patient" />
                                <TableColumn fx:id="notesColumn" prefWidth="220.0" text="Reason" />
                                <TableColumn fx:id="statusColumn" prefWidth="120.0" text="Status" />
                            </columns>
                        </TableView>

                        <!-- New bottom button bar under the table -->
                        <HBox spacing="10" alignment="CENTER_RIGHT">
                            <children>
                                <Region HBox.hgrow="ALWAYS" />
                                <!-- Create new slot -->
                                <Button text="Create appointment"
                                        onAction="#onCreateAppointmentClicked" />
                                <!-- Make selected slot AVAILABLE again -->
                                <Button fx:id="makeAvailableButton"
                                        text="Make available"
                                        onAction="#handleMakeAvailable" />
                                <!-- Cancel selected slot -->
                                <Button fx:id="cancelAppointmentButton"
                                        text="Cancel appointment"
                                        onAction="#handleCancelAppointment" />
                                <!-- Delete selected slot -->
                                <Button fx:id="deleteAppointmentButton"
                                        text="Delete appointment"
                                        onAction="#handleDeleteAppointment" />
                            </children>
                        </HBox>

                    </children>
                </VBox>

            </children>
        </VBox>
    </center>

</BorderPane>
